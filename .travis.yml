sudo: required
services:
  - docker

before_install:
  - docker build -t bcochranswi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bcochranswi/docker-react npm run test -- --coverage
